# S3 backend configuration — update bucket to your existing Terraform state bucket
# Usage: terraform init -backend-config=backend.tfvars

bucket = "aws-ai-study-assistant-tfstate"
region = "eu-west-1"
